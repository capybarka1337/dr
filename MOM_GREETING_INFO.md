# Специальная страница поздравления для мамы

## 🌹 Описание

Создана специальная скрытая страница с уникальным поздравлением для мамы. Эта страница:

- **Доступна только по прямой ссылке** (не показывается на главной странице)
- **Без указания темы** (нет бейджа с темой)
- **Без кнопок** "Создать своё поздравление", "Поделиться" и других элементов управления
- **Уникальный дизайн** с летающими сердечками, конфетти и плавными анимациями

## 🔗 Как получить доступ к странице

### Для локального тестирования:
```
http://localhost:5173/?dr=best
```

### Для продакшн версии:
```
https://ваш-домен.com/?dr=best
```

## ✨ Особенности

- Красивый градиентный фон в нежных розово-фиолетовых тонах
- Анимированные летающие сердечки ❤️
- Эффект конфетти при загрузке
- Плавное появление текста строка за строкой
- Пульсирующая иконка розы 🌹
- Адаптивный дизайн для всех устройств
- Поддержка `prefers-reduced-motion` для пользователей с ограниченными возможностями

## 📝 Текст поздравления

Текст поздравления включает теплые слова благодарности и любви к маме. Если нужно изменить текст, отредактируйте файл:

```
src/components/MomGreeting.tsx
```

В массиве `greetingText` (строки 12-26).

## 🎨 Настройка дизайна

Все стили находятся в файле:

```
src/index.css
```

В секции `/* Mom Greeting Styles */` (начиная со строки 1425).

## 🔐 Безопасность

Параметр `dr=best` является специальным ключом доступа к странице. Вы можете изменить его на любой другой в файле `src/App.tsx` (строка 22):

```typescript
if (specialPage === 'best') {
```

Измените `'best'` на любое другое значение для дополнительной приватности.

---

**С любовью создано для самой лучшей мамы! 💝**
